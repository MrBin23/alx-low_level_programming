#include <stdio.h>
#include "main.h"
/**
 * main - prints args
 * @argv: command line args
 * @argc: arg number
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int j;

	for (j = 0; j < argc; j++)
	{
		printf("%s\n", argv[i]);
	}
	return (0);
}
